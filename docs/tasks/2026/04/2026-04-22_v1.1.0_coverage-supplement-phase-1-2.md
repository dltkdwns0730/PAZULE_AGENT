---
date: 2026-04-22
version: v1.1.0
purpose: "설정(Config) 및 위원회 심판(Council Judge) 테스트 보완을 통한 커버리지 향상"
status: "IN_PROGRESS"
author: "Gemini CLI"
parent_task: "v1.0.0_setup-pm-orchestration"
---

# 📋 작업 명세서: 테스트 커버리지 보완 - Phase 1 & 2A

## 1. 배경 및 목적
- **배경:** `TDD_VALIDATION_PLAN.md` 분석 결과, 핵심 모듈의 테스트 누락 및 낮은 커버리지 확인.
- **목적:** 설정 모듈 커버리지 100% 달성 및 `app/council/judges.py` 로직의 정확성 검증.

## 2. 구현 세부 사항
- [x] `tests/core/test_settings.py` 생성: 환경별 프로필 전환 및 우선순위 로직 검증 완료.
- [x] `tests/council/test_judges.py` 생성: ConsistencyJudge 및 ThresholdJudge의 판정/에스컬레이션 로직 검증 완료.
- [x] `tests/council/test_aggregator_tiered.py` 생성: `tiered_evaluate` 단계별 에스컬레이션 로직 검증 완료.
- [x] `tests/council/test_deliberation_node.py` 생성: 위원회(Council) 노드의 활성/비활성 및 연동 로직 검증 완료.
- [x] `tests/api/test_routes.py` 생성: 힌트, 미션 시작, 쿠폰 사용 등 핵심 API 엔드포인트 검증 완료.

## 3. 검증 결과 및 품질
- **테스트:** `uv run pytest tests/core/test_settings.py` (통과: 7/7)
- **테스트:** `uv run pytest tests/council/test_judges.py` (통과: 9/9)
- **확인:** 설정 프로필 스위칭 및 심판 에스컬레이션 트리거가 격리된 테스트 환경에서 의도대로 동작함을 확인.

## 4. 핸드오버 참고 사항
- **다음 단계:** Phase 2B/2C (Aggregator 및 Deliberation)를 완료하고 Phase 3 (API Routes)로 진입.
- **특이 사항:** `aggregator` 로직은 `nodes.py` 내부에 있으며, `aggregator.py`는 `tiered_evaluate`를 담당함.
